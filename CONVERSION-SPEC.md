# 课程教程站 · 批量转化规范 v1.0

> 适用范围：`tutorial-site/` 下第 6 次课至第 32 次课（共 27 课）的页面转化。
> 基准模板：`lessons/lesson-06-ollama.html`（基准模板 A）、`lessons/lesson-07-workbuddy.html`（基准模板 B）。
> 新增一课之前，请先完整读一遍本文件；写完必须过 `node tools/check_site.js <课次号>`。
> 制定日期：2026-09-15

---

## 一、先划清边界：这是教程站，不是课堂底本

这一条最重要，写在最前面。

| 项目 | 只出现在备课材料里（教案 / 逐字稿 / 学习通） | 出现在教程站 |
|---|---|---|
| 学习通题号、计分口径、几分几题 | ✅ | ❌ **一律不写** |
| 时间账（讲授 X 分钟 / 学生活动 Y 分钟） | ✅ | ❌ **一律不写** |
| 环节编号（环节 1 / 环节 2）、`【讲】【练】【切PPT】` 标记 | ✅ | ❌ **一律不写** |
| 环境兜底、降级方案、举手统计等课堂组织动作 | ✅ | ❌ **一律不写** |
| 原理与机制讲解、命令与代码、参数对照 | ✅ | ✅ **是主体** |
| 动手步骤、常见坑位、验收方式 | 部分 | ✅ **要写全** |
| 自测题 | 学习通客观题 | ✅ **改写为「不计分自测 + 答案解析」，与学习通题面不重合** |

**判据**：如果一段文字的作用是「让这 80 分钟顺利走完」，它不属于教程站；如果作用是「让人课后照着能自己学会」，它属于教程站。

---

## 二、目录组织

```
tutorial-site/
├── index.html                        首页 · 课程地图（由 course-map 驱动）
├── README.md                         项目说明：怎么打开、怎么加课
├── CONVERSION-SPEC.md                本文件：批量转化规范
├── assets/
│   ├── css/site.css                  全站唯一设计系统，禁止在课时页写行内样式
│   ├── js/course-map.js              ★ 全课程数据源（导航 / 首页 / 上下课全靠它）
│   ├── js/search-index.js            ★ 章节级搜索索引（每课转化后必须追加）
│   ├── js/site.js                    运行时引擎：导航、目录、搜索、代码高亮与复制
│   └── img/                          图片（优先用内联 SVG，尽量不放位图）
├── lessons/
│   ├── lesson-06-ollama.html         基准模板 A（实操课）
│   ├── lesson-07-workbuddy.html      基准模板 B（理论课）
│   ├── lesson-08-skill.html          第 8 次课 Skill（实操）
│   ├── lesson-09-experts.html        第 9 次课 专家与专家团（实操）
│   ├── lesson-10-word.html           第 10 次课 Word 文档生成与批量处理（理论）
│   └── lesson-NN-slug.html           后续课时，一课一文件
├── templates/
│   └── lesson-template.html          页面骨架，新课时从这里复制
└── tools/
    ├── check_site.js                 质量校验（结构 / 锚点 / 索引 / 语法 / 冒烟 / 版式）
    ├── new-lesson.js                 一键新建课时页脚手架
    └── serve.py                      本地预览服务器（禁用缓存，别用 python -m http.server）
```

### 命名规范

| 对象 | 规则 | 例 |
|---|---|---|
| 课时文件名 | `lesson-<两位课次号>-<英文短slug>.html` | `lesson-11-excel-a.html` |
| 小节 id | `s-<语义名>`，全小写、连字符分隔 | `s-install`、`s-tier` |
| 小节内三级标题 id | `s-<小节名>-<子语义>` | `s-pit-1`、`s-pit-2` |
| 演示容器 id | `demo1`、`demo2`…**按演示在页面里出现的先后顺序递增**，可见的 `① ② ③` 必须与 id 数字一致 | `demo3` |
| 演示内元素 id | `d<演示号>-<语义>`，演示号与所属 `demoN` 一致 | `d2-mem`、`d4-log` |
| 演示内函数名 | `d<N><动作>`，与所属 `demoN` 一致（如 `d4Plan`、`d1Show`） | `d4Plan` |

> **三级标题（h3）默认不加 id。** 只有需要从别处单独跳转（例如知识点导航要直接链到某个小节内部的子标题）时才给 h3 加 id。
> 第 6–10 次课均未给 h3 加 id；`check_site.js` 的索引一致性只比对 `section.sec` 的 id，h3 加不加都不影响校验，加多了反而要额外维护。

---

## 三、页面结构规范

### 3.1 固定骨架（六个基本小节，顺序不可变）

| 顺序 | 小节 id | 标题 | 内容要求 |
|---|---|---|---|
| 1 | `s-guide` | 本课导读 | 一段 `p.lead` 讲清这课解决什么真实问题 + 2–4 条具体现象/问题 + 一个「提前说清楚」的 warn 提示框 |
| 2 | `s-goal` | 学习目标 | `div.goal` 装 4–6 条可观察的目标 + `h3 知识点导航` 的 `div.knav` 卡片 + 一个「先修与衔接」的 quote 提示框 |
| 3 | `s-…` ×2–8 | 正文小节 | 见 3.2 |
| 4 | `s-quiz` | 随堂自测 | 5–8 题，每题必须有解析，解析要讲清「为什么错」 |
| 5 | `s-recap` | 本课要点回顾 | `div.recap`（深色卡）+ 一段「下一步棋」衔接下一课 |
| 6 | `s-hw` | 课后作业 | 任务描述 + 交付清单 + 一个「评分看什么」的 tip 提示框 |

### 3.2 正文小节的内部写作顺序（固定四段式）

每个知识点一节，节内按这个顺序组织，不要跳步：

1. **现象 / 痛点切入**——先用「你身边就能看到的事」引入，不要一上来下定义；
2. **机制讲清**——给定义，配 `table.data` 对照表，能用表格就不用段落；
3. **操作 / 代码**——命令或代码，能现场跑的就给完整可运行版本；
4. **提醒或结论**——`callout` 收口，读者读完知道「所以我要注意什么」。

每节只讲透**一个**概念。如果你发现一节里有三个并列的「另外，……」，拆成三节。

### 3.3 页面里不要写的结构

以下结构由 `site.js` 自动生成，**课时页里禁止手写**，否则会重复：

- 顶栏（`#site-topbar`）、左侧课程目录（`#site-sidebar`）
- 面包屑（`#site-crumb`）、页面标题区（`#site-head`）
- 本页目录（`#site-toc`）、上下课导航（`#site-pager`）、页脚（`#site-foot`）

课时页 `body` 只写两个属性 + 三个容器 + 内容区：

```html
<body data-lesson="8" data-root="../">
  <div id="site-topbar"></div>
  <div class="layout">
    <aside class="sidebar" id="site-sidebar"></aside>
    <main class="main" id="main">
      <div id="site-crumb"></div>
      <div id="site-head"></div>
      <!-- ↓ 只写这一段 ↓ -->
      <section class="sec" id="s-guide">…</section>
      …
      <!-- ↑ 只写这一段 ↑ -->
      <div class="pager" id="site-pager"></div>
    </main>
    <aside class="toc" id="site-toc"></aside>
  </div>
  <footer class="foot" id="site-foot"></footer>
  <script src="../assets/js/course-map.js"></script>
  <script src="../assets/js/search-index.js"></script>
  <script src="../assets/js/site.js"></script>
  <script>/* 本课专属演示逻辑 */</script>
</body>
```

---

### 3.4 实操任务区（本站重点，**每一课必备**）

> 2026-09-17 教师明确要求：「针对性的实操还是不够……要有一些实在的实操项目，不要太难，
> 并且要帮我准备好实操项目及资料。」
> 随后又补充：「**实操任务最好是分散开来，不要全放在课程的最结束**。
> 最好是讲一部分知识、做一个对应的实操，再讲一部分、再做一个实操。」

本站定位是**实操手册**，不是教材——只有流程演示、没有能动手的任务，等于没落地。

#### ① 实操要「穿插」，不能堆在最后

**一个知识点 + 一个对口实操**，交替推进。用 `div.task` 承载，紧跟它对应的那一节。

```
一、Skill 是什么   → 实操① 认一认手上的技能（3 分钟）
二、它是怎么工作的  → （纯概念，不配实操）
三、找到 Skill     → 实操② 装一个真正需要用的（5 分钟）
四、用它改一篇稿子  → 实操③ 把「AI 味」稿子改掉（15 分钟，本课主体）
五、关闭与卸载     → 实操④ 关掉再打开（3 分钟）
                   → 实操⑤ 动手造一个技能（挑战，8 分钟）
```

> 反面例子（已推翻的做法）：把三个任务打包成一节「五、动手做」堆在课程末尾。
> 学生前面全是听，最后集中练——注意力已经散了。

#### ② 实操 = 学生**真的动手做出一个东西** ⚠️⚠️ 最容易理解错的一条

> 2026-09-18 教师纠正：「实操是要给学生一个具体的任务去做，**不是让学生填这种空**。
> 就是要让学生**实际地操作 WorkBuddy，去完成任务**。」

**判据只有一句：学生做完之后，手上是不是多出一样东西？**

| ✗ 填空型（已被明确否决） | ✓ 任务型 |
|---|---|
| 「我装的是 ____，它能 ____」 | 装一个技能，**用它把一个 PDF 转成 Word**，交出转换后的文件 |
| 「找出空话词，填进表格」 | 拿一篇 AI 味稿子，**改到能发出去**，交初稿 + 修订稿 |
| 「说说它的不足」 | **创建一个专家并用它处理一份真实材料**，交专家 + 处理结果 |

**三件必须做到的事**：

1. **必须有产出物**——文件、成果、一份专业意见。**验收看产出物，不看学生填的字。**
2. **必须备好素材**，放进可复制文本块让学生一键拿走。实测最有效的两类：
   - 「**一份有明显问题的成品**」：空话连篇的稿子、字段错位的表格；
   - 「**一份待处理的原始材料**」：杂乱的会议记录、待审的协议条款、待整理的文件夹清单。
   **光有任务描述、没有素材，学生动不了手。**
3. **必须给现成的指令模板**（可复制），学生照着发出去就行——
   他此刻要学的是「怎么用 AI 做事」，不是「怎么憋指令」。

**难度分层**：若干短线小任务 + 1 个挑战；挑战的完成标准可以是「**能指出一处不足**」。
**先手做、再机做**：先让学生用自己的脑子做一遍，再用 AI 做，最后对比——**对比本身就是教学**。

#### ②-b 具体 ≠ 填空

> 教师补充：「实操的任务最好是**再具体一点、再具体一点**。」

「具体」指**操作路径和素材要明确**，不是让学生填表：

| 别这样写（虚） | 要这样写（照做级） |
|---|---|
| 「随便挑三个」 | **指定**看哪些；或给**可勾选候选清单** |
| 「找出空话词删掉」 | **把空话词列成清单**（十几个），学生照着圈 |
| 「想一件要做的事」 | **三个可勾选候选** + 一句选事标准 |
| 「描述一下你的需求」 | 给**句式模板**（填完横线就能发）+ 一个**完整示范** |

三条硬要求：**① 指定对象**；**② 给清单或模板**；**③ 给合格示例**（「✗ 不合格式 vs ✓ 合格」对照）。

> ⚠️ **清单和模板是用来支撑操作的，不是用来替代操作的。**
> 一旦整节课学生只是在填表、从没打开过 WorkBuddy，就是走偏了。

#### ②-c 实操块的固定写法（照做级）⚠️ 2026-09-20 定，后续所有课一律照此执行

> 2026-09-20 教师第三次强调：「要具体的实操过程及实操内容，**不要什么实操记录**，
> 我们重要的是实操过程。要把实操过程所需要的步骤，以及这些提示词、该复制的一键复制写清楚，
> 让学生能够跟着步骤完成实操任务。」
>
> 本条是对 ②、②-b 的**落地细化**——前面解决「要不要」，这里解决「长什么样」。

**每个 `div.task` 固定四段式，一段都不能少：**

```
① 产出物一句话  → 「做完你手上会有：一份 XXX」，开门见山告诉学生终点在哪
② 素材 / 准备   → 必须给。放进 codeblock--plain，data-name 里写明「点右上角复制」
③ 分步操作      → 每步一个「第 N 步 · 标题（X 分钟）」+ ol.steps；
                   步骤里凡是要学生输入的内容，一律另配可复制块，不让手打
④ 验收          → 「做到 X，就算做完了」+「没做到怎么补救」的陈述式清单。
                   看产出物，**不看学生写了什么字**。
                   ⚠️ 教师 2026-09-20 追加：**验收不要勾选、不要记录**。
```

**四条硬红线（踩中任意一条 = 不合格，检查脚本不查、靠人工查）：**

| ✗ 不许出现 | 为什么不行 | ✓ 改成 |
|---|---|---|
| `答：______`、`我装的是 ____` 这类填空横线 | 规范 ② 里已被明确否决 | 给**完整可复制指令**，或「✗/✓ 合格对照」示例 |
| 「我的发现」「记录一下」空白表格 | **记录 ≠ 操作**，写完字学生手没动过 | 「做到 X 就算对」的陈述句 |
| 验收用 `□` 打勾清单 | 勾选本质上也是记录，学生只是在表单上打钩 | 同上；每条补一句「<em>没做到</em>：该怎么补救」 |
| 「写下你的发现 / 各写一句结论」 | 让学生写字，不是让他做事 | 「找出一处问题，说清它在哪一份的哪一处」 |
| 只有任务描述、没有素材 | 没材料，学生根本动不了手 | 补可复制素材块（一份「有问题的成品」或「待处理原始材料」） |
| 「自由发挥」「随便挑一个」 | 不够具体，学生卡在第一步 | 给 **3 个可勾选候选** + 一句选择标准 |

**写指令（提示词）时的三个要素**，缺一不可：

1. **要它做什么** —— 具体到输出形式（表格 / 清单 / 几段话）
2. **不许做什么** —— 红线写死（「不许编造」「不许改原文件」「不知道的写待定」）
3. **缺信息怎么办** —— 明说标成 `【待补】`，而不是让它猜

> 给学生现成指令，是因为他此刻要学的是「**怎么用 AI 做事**」，不是「怎么憋指令」。

**易错点要显式标出来**，用 `<span class="task__warn">`（白底描边徽章，已定义在 `site.css`）：
典型如「必须新建对话」「整句粘贴、不要自己改写」「这一步最容易被记错」。
这类提示放在它对应的那一步里，不要攒到末尾。

**篇幅参考**：短线小实操 4–6 步 / 约 60–110 行 HTML；本课主题实操可到 150 行以上。

#### ③ 课后作业：客观题 + 逐题带答案解析 + 一键复制

> 2026-09-17 教师要求：「作业不要这些填空题，简单一点，就是选择、判断、多选题这种题型。」
> 2026-09-18 补充格式：「**首先是第一题题目和选项，然后下面出该题的答案与解析**。」

**固定版式（照抄，不要改）**：

```
1.（单选）一个 Skill 文件夹里，哪个文件是必需的？
A. scripts 目录
B. SKILL.md
C. references 目录
D. assets 目录
【答案】B
【解析】只有 SKILL.md 是必需的，另外三个目录都可选。它写明用途、输入、输出和步骤，其中 description 决定了 AI 什么时候会想起用这个技能。

2.（多选）下面哪些方式可以在 WorkBuddy 里拿到技能？
A. ……
【答案】ABCD
【解析】……
```

**五条硬要求**：

1. **不要分隔标题**。❌ 不要 `━━━ 第 1 题（单选）━━━` 这类行——教师明确说过「不要有多余的标题」。
2. **块内也不要标题行**。❌ 连 `【第 N 次课作业】单选 / 多选 / 判断 · 共 6 题` 这种开场行都不要——
   **直接从第 1 题开始**。块的身份由 `data-name`（顶部栏那个小标签）体现即可，不占内容区。
3. **题号后直接跟题型和题干**：`1.（单选）题干……`（题型标在题号后，不单独成行）。
4. **选项顶格、不缩进**：`A. 选项`（不要写成 `　A. 选项`）。
5. **答案和解析紧跟题干块**：`【答案】X` 换行接 `【解析】……`，**中间不空行**。
   题目与题目之间空一行。

**其他要求**：
- 题型**只出单选 / 多选 / 判断**。
- **解析要讲清「为什么错」**，不能只给答案——这一点和随堂自测一致。
- **作业块默认折叠**（2026-09-18 教师要求：「课后作业那部分的内容默认处于折叠状态，
  单击一下它才会展开」）。用 `div.fold` 包住 `codeblock--plain`：

```html
<div class="fold">
  <button class="fold__hd" type="button">点开查看作业 · 单选 / 多选 / 判断 · 共 6 题</button>
  <div class="fold__bd">
    <div class="codeblock codeblock--plain" data-lang="text" data-name="第 N 次课作业 · 一键复制">
      <div class="codeblock__bar"></div>
<pre><code>1.（单选）……
</code></pre>
    </div>
  </div>
</div>
```

> 页面上的正文引导语也随之改成「**点一下展开**，再点框内右上角「复制」就能整段拿走」。
> 折叠状态由 `site.js` 的 `bindFold()` 切换 `.fold.on` 控制，无需额外 JS。

- **与「随堂自测」不重复**：自测偏概念理解（供课后自查），作业偏操作与应用（供提交）；
  两套合计 **10 题左右**。
- ❌ 不要填空式模板（`【名称】____`）——已被明确否决。

#### ④ 篇幅参考

一次课 80 分钟，全部实操合计应占 **30–40 分钟**。
若实操内容比原理讲解还少，说明还没到位。

---

## 四、章节排版样式规范（可用类清单）

**只允许使用 `site.css` 里已有的类，禁止在正文写行内 `style`。** 校验脚本会把行内样式报为警告。

| 用途 | 类名 | 用法要点 |
|---|---|---|
| 内容小节 | `section.sec` + `id="s-xxx"` | 锚点 id 落在 section 上，`h2` 不带 id |
| 小节标签 | `span.sec__tag` | 放在 h2 末尾，如「本课核心」「模块二开局」 |
| 导读段 | `p.lead` | 16px 浅色，每节最多一个 |
| 小字备注 | `p.note` / `span.note` | 出处说明、口径说明、教学示意声明 |
| 学习目标框 | `div.goal` | 内放 `h4` + `ol` |
| 知识点导航 | `div.knav` > `a` | 每条含 `<b>01</b>标题` |
| 提示框 | `div.callout` | 四型见下 |
| 提示框四型 | `--tip` 绿（经验）<br>`--warn` 橙（注意）<br>`--danger` 红（风险/红线）<br>`--quote` 灰（金句/结论） | 标题用 `<span class="callout__t">` |
| 表格 | `div.tablewrap` > `table.data` | **必须包 tablewrap**，否则窄屏溢出；两列内用 `table.data.tight` |
| 双栏对比 | `div.compare` > `div.compare__col`（右列加 `.b`） | 用于「旧做法 vs 新做法」这类二选一 |
| 步骤条 | `ol.steps` > `li` > `b` + `span` | 有先后顺序的操作流程 |
| 代码块 | `div.codeblock[data-lang][data-name]` | 见第五节 |
| 折叠区 | `div.fold` > `button.fold__hd` + `div.fold__bd` | 放「多套可选模板」这类不必一次铺开的内容 |
| 徽标 | `span.badge`（`--p` 蓝 / `--a` 橙 / `--ok` 绿 / `--todo` 黄） | 行内小标签 |
| 键位 | `<kbd>` | 快捷键 |
| 要点回顾 | `div.recap` > `h4` + `ol` | 深色底，关键词加 `<b>` |
| 小字行内码 | `<code>` | 正文里的命令、字段名 |
| 勾选清单 | `ul.checklist` > `li` > `label` + `input[type=checkbox]` | 逐条判断类演示（审校卡、审查卡）。标签后可挂 `span.badge` 显示判断结果 |
| 当前节点高亮 | `.flow__node.on`（进行中）/ `.flow__node.dim`（未开始或已完成） | 配合「逐步播放」的流程图演示 |
| 课次引用 | `<a class="xref" data-lesson="8">` | **正文里指向其他课次一律用它，不要写死链接**。链接与标题由 `site.js` 按 `course-map` 生成；已上线可点，未上线自动显示「待上线」 |
| 配图 | `figure.figure` > `img` + `figcaption` | 界面截图/示意图统一容器。**图注必须写在 `figcaption` 里**，并注明来源与「界面随版本更新，以实际为准」 |
| 图集 | `div.fold` > `button.fold__hd` + `div.fold__bd`（内放多个 `figure`） | 同主题的连续过程图（如「创建任务的 8 步」）收进折叠区，不挤占正文版面 |
| 可复制文本块 | `div.codeblock.codeblock--plain`（结构同 `.codeblock`） | **非代码但要一键复制**的内容：作业要求、待处理素材、记录卡模板。结构照抄 `.codeblock`，复制按钮由 `site.js` 自动注入，无需额外 JS |
| 课堂实操块 | `div.task` > `div.task__hd`（含 `<em>` 标时长）+ 正文 | **穿插在知识点之间的动手环节**。橙色调，学生一眼能看出「这里要动手」。写法见 §3.4 |

### 文案质量三条

1. **不用「了解」「掌握」「熟悉」写目标**——改成「能说出」「能独立完成」「能判断」。
2. **禁止 AI 腔**：「值得注意的是」「综上所述」「让我们一起」一律删掉，像同事一样平实说话。
3. **比喻要生活化**：面向高职学生，「自己家厨房 vs 点外卖」「谁的孩子 + 家规」这类比喻比定义有效。

---

## 五、代码展示规范

### 5.1 代码块结构（照抄）

```html
<div class="codeblock" data-lang="bash" data-name="PowerShell / 终端">
  <div class="codeblock__bar"></div>
<pre><code>> ollama pull qwen3:1.7b
</code></pre>
</div>
```

- `codeblock__bar` **留空**，语言标签与复制按钮由 `site.js` 注入；
- `data-lang` 必填，可取：`bash` `powershell` `cmd` `python` `json` `sql` `ini` `modelfile` `text`；
- `data-name` 选填，写「文件名」或「这是在哪执行的」；
- **行首 `> ` 会被渲染成绿色提示符**，用于区分「你敲的」和「它回的」；
- `<pre><code>` 紧贴行首书写，保证第一行为代码本身；
- 内容里的 `<` `>` `&` 必须写成 `&lt;` `&gt;` `&amp;`。

### 5.2 代码本身的三条硬要求

1. **Python 必须完整可运行**，给中文注释、说明依赖（`pip install xxx`）与运行前提；
2. **命令类代码块给出「预期输出」**，让学生知道什么算成功（见第 6 课「终端实录」）；
3. **破坏性操作必须显式提示风险**，并且默认写成「只读 / 预演」版本（见第 7 课 `scan_and_plan.py`，只扫描不移动）。

---

## 六、可运行演示规范

### 6.1 硬性要求

- **纯前端、零依赖、断网可用**：只用原生 JS + CSS，不引入任何 CDN；
- **确定性**：演示结果不能依赖网络与随机数，必须可复现；
- **示意数据必须标注**：凡非实测数据，在 `div.demo__note` 里写明「教学示意，非实测数值」；
- **每个演示必须有一句「这个演示让人看懂什么」**写在 `demo__title` 里；
- 演示逻辑写在页面最后的 `<script>` 里，用 `var $ = function(id){...}` 简写取节点。

### 6.2 可复用组件（全部在 `site.css`）

| 组件 | 类名 | 适合表达 |
|---|---|---|
| 流向图 | `.flow` > `.flow__node` / `.flow__path` / `.flow__dot`（`.run` `.bad` `.hold`） | 数据从哪到哪、断网会怎样 |
| 指标卡 | `.kpis` > `.kpi` > `b` + `span` | 参数变化后的结果对比 |
| 键值清单 | `dl.kvlist` > `dt` + `dd` | 解释性字段 |
| 终端日志 | `.demo__out.log`，行内用 `.ok` `.no` `.dim` `.hl` | 逐行输出、执行过程 |
| 界面模拟 | `.mock` > `.mock__col` / `.mock__sub` / `.mock__sub-hint` / `.mock__chip`（`.mock__bar` 用于整行的栏状区域） | 讲清软件界面分区；`.mock__sub` 表示某一区内部的控件行 |
| 按钮 | `.btn`（`--main` `--primary` `--accent` `--ghost`）、`.btngroup` + `data-tab` | 操作与切换 |
| 滑块 | `.range` + `input[type=range]` | 连续参数（内存、步数、强度） |

### 6.3 演示中的 JS 工具箱

```js
window.Site.esc(str)                 // HTML 转义，拼接文本时必用
window.Site.highlight(code, lang)    // 语法高亮，返回 HTML 字符串
window.Site.type(node, lines, done)  // 逐行「打字」输出到 node，数组元素可含 HTML
```

`Site.type` 的 `lines` 支持行内标签：`<span class='ok'>✓ 成功</span>`、`<span class='no'>✗ 失败</span>`、`<span class='dim'>次要信息</span>`、`<span class='hl'>重点</span>`。

---

## 七、数据源维护规范

### 7.1 `assets/js/course-map.js`

- 全站**唯一**导航数据源。改这里，左侧栏、首页课程地图、首页快捷入口、顶部课次下拉、上下课导航、搜索同时生效；
- 课的 `no` **必须等于授课计划表中的实际课次号**（一定要去 `《AIGC应用与实践》授课计划表.docx` 核对，不要按数组下标推）；
- 转化完成一课，把该课 `status` 由 `"pending"` 改为 `"ready"`；
- **`index.html` 不需要手改**：首页的「怎么用这个站」入口列表由页面脚本按 `course-map` 自动生成，
  最近上线的课排前面（超过 6 课自动折叠为「其余 N 课见下方课程地图」）。
  换句话说，**改完 `course-map` 的 `status`，首页入口就自动出现了**——不要再手动往 `index.html` 里加课次链接。
- **正文里的课次引用一律用 `<a class="xref" data-lesson="N">`，不要写死课次链接或标题。**
  链接与标题由 `site.js` 按 `course-map` 自动填充：**已上线的课变可点链接，未上线的自动降级为带「待上线」标记的纯文字**（不会产生死链）。
  例：`<a class="xref" data-lesson="8"></a>` → 渲染成「第 8 次课 Skill——给 AI 装操作手册」并接上链接；
  也可以自带文字：`<a class="xref" data-lesson="18">连接器那一课</a>`。
  好处是——调整课次标题、或以后把新课上线时，**正文一个字都不用改**。
- 数据一律用中文写清楚，禁止出现「待补充」「TODO」之类的占位文案。

### 7.2 `assets/js/search-index.js`

- 每转化一课，追加该课全部小节条目；
- `id` 必须是页面上真实存在的 id，写完用校验脚本核对（脚本会双向比对）；
- `hint` 写这一节到底讲什么，堆关键词但保持可读——它是搜索命中率的唯一来源。

### 7.3 校验脚本

```bash
node tools/check_site.js            # 校验全部已上线课时
node tools/check_site.js 11         # 只校验第 11 次课
node tools/check_site.js --quiet    # 只输出问题
```

检查项与通过标准：

| 项 | 检查内容 | 通过标准 |
|---|---|---|
| A 结构 | course-map 数据完整性 | 课时 27、课次无重复、学时 54 |
| B 契约 | `data-lesson` / `data-root` / 必需容器与脚本引用 | 全部齐全且一致 |
| C 锚点 | 页内锚点、id 唯一性、小节命名 | 无失效锚点、无重复 id |
| D 索引 | search-index 与页面小节双向一致 | 无悬空、无遗漏（遗漏仅警告） |
| E 语法 | 所有内联脚本与站点 JS | 语法全部通过 |
| F 冒烟 | jsdom 真跑：侧栏/目录/复制按钮/高亮/搜索，遍历点击所有 button、拖动所有 range 到 min/中值/max | **零运行时错误** |
| G 版式 | `data-lang` 齐全、表格包在 tablewrap 内、正文无行内样式 | 无 FAIL |

**判定**：`失败 0` 才算通过。警告需逐条判断，可接受的（如「未进搜索索引」）需在提交说明里写清理由。

### 7.4 本地预览：用 `tools/serve.py`，别用 `python -m http.server`

**踩过的坑（2026-09-17）**：数据源明明改好了（第 8–10 课 `status` 都是 `ready`），
本地渲染实测也正确，但使用者反馈「侧栏里第 8 次课及之后**点不开**」——
排查到最后是**浏览器缓存了旧的 `course-map.js`**（那时第 8–10 课还是 `pending`）。

- `python -m http.server` 会返回 `Last-Modified`，浏览器据此缓存 JS；
  改了数据源之后刷新，仍可能用旧版，**症状和真 bug 一模一样**。
- 解法：`python tools/serve.py`（默认 8806）。它给所有响应加
  `Cache-Control: no-store, no-cache, must-revalidate, max-age=0`，刷新即生效。
- 已启动的旧服务要**先停掉**再换，否则端口占用启动失败
  （查 PID：`netstat -ano | grep ":8806" | grep -i listening`；停：`taskkill /F /PID <PID>`）。

**自查办法（推荐写进交付说明）**：页脚会显示

```
… | 课程教程站 v1.0 | 已上线 5 / 27 课
资料版本 2026-09-17 · …
```

「已上线 N / 27」与「资料版本」对不上最新状态，就是加载了缓存。
按 <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>R</kbd> 强制刷新一次即可（之后 `no-store` 会接管）。

> **通用教训**：凡是「改完数据源但界面没变」的反馈，**先排除缓存，再怀疑代码**。
> 手写的清单、浏览器缓存、构建产物——这三样都不会报错，只会静悄悄地过期。

---

## 八、单课转化 SOP（照这个顺序做，别跳步）

```
① 读源材料
   05_正式教案.md + 课堂材料包/逐字稿.md + 参考资料/*.md + PPT大纲.md

② 提取「可教学化」内容
   按第一节的边界表过滤：去掉题号/计分/时间账/环节标记，
   留下原理、机制、命令、坑位、案例

③ 补事实
   凡是年份、版本、论文、产品功能、界面名称 —— 先联网查证，
   写进一个「事实来源」备注，无法证实的写进 99_待确认清单.md，不进页面

④ 设计 2–8 个正文小节
   每节一个概念，按「现象 → 机制（表） → 操作（码） → 提醒」四段式

⑤ 设计 2–4 个可运行演示 + 1–2 段完整可运行代码
   优先选「看不见的机制」做演示（数据流向、参数影响、执行流程）

⑥ 写自测题与作业
   自测题与学习通题面不重合；作业写清「交什么 + 评分看什么」

⑦ 登记数据源
   course-map.js 改 status: "ready"
   （首页快捷入口 / 首页课程地图 / 左侧目录 / 课次下拉 / 上下课导航 / 搜索 全部随之自动更新，
     不需要动 index.html）；search-index.js 追加该课全部小节

⑧ 过校验
   node tools/check_site.js <课次号>   → 失败 0 才算完成
```

### 开工命令

```bash
node tools/new-lesson.js 8
```

它会生成 `lessons/lesson-08-*.html` 并打印待办清单。

---

## 九、内容准确性铁律

1. **不确定就说不确定**。页面上宁可写「这一点我不确定，请以官方文档为准」，也不许含糊带过。
2. **凡涉及产品界面、版本、政策、时间节点，必须联网核实并标注核对日期**。范例见第 7 次课对 WorkBuddy 三种模式的写法：界面名称取自官方文档，模式名称来自输入框菜单，页面上明确标注「本站核对日期 2026-09」。
3. **示意数据必须就地声明**。「教学示意，非实测数值」这句话不能省。
4. **不编造文献、案例、数据来源**。引用到的口径要能指回原始文件或原始网页。
5. **课堂要素不进页面**（题号、计分、时间账、环节编号），但**教学内容必须与教案一致**——同一件事，教案和教程站的说法不能打架。

---

## 十、口径确认与待确认清单

### 10.1 已确认口径（2026-09-15 教师确认，全站照此执行）

| # | 事项 | 确认结论 |
|---|---|---|
| 1 | 模块起止课次 | **按自洽口径**：《授课安排.docx》的课时分配表写「模块二 第 7–23 次（34 课时）／模块三 第 24–32 次（18 课时）」，与之自洽；文档正文把「模块三」标题放在第 22 次课之前属笔误。**采用 模块一 1–6、模块二 7–23、模块三 24–32**，`course-map.js` 已照此填写 |
| 2 | 界面分区 | **按官方文档口径分三区**：侧边栏 / 对话区 / 结果区。输入框及其上方那一排功能入口（模式、专家、技能、连接器、@ 引用、添加文件）与左下角「选择工作空间」**均属于对话区，不单列**。第 7 次课页面（含导览演示、逐区说明表、导航与页面摘要）已全部改为三区表述 |

> ⚠️ **待同步（不在本站范围内）**：`AIGC应用与实践_32次课授课安排.docx` 第 7 次课条目里写的是「熟悉界面**四大区域**」。
> 本站已统一为三区口径，教案与授课安排文档中的对应表述需要任课教师自行同步，避免两处说法打架。

### 10.2 仍待确认（转化时按此处理）

| # | 事项 | 现状与处理 |
|---|---|---|
| 1 | 学习通章节对应关系 | 第 7 次课明确对应 2.1，之后「一一对应」，据此推定为 第 N 课 → 2.(N−6) / 3.(N−23)。**未在正式材料中逐条核实**，`course-map.js` 中按推定填写，用「（推定）」标注 |
| 2 | 第 6 次课的学习通章节 | 源材料写「暂无对应章节」，本站原样保留 |
| 3 | 新增外部工具的事实核对 | 第 27–29 次课涉及 Codex / Hermes-Agent / DeepSeek-Harness，均为快速迭代工具，转化前必须逐个联网重新核实，并在页面标注核对日期 |

---

## 十一、官方文档来源清单（转化时优先查这里，别每次重搜）

转化涉及产品功能的课时之前，先在本节找对应页面。**引用时页面上要标注核对日期。**

| 主题 | 官方页面 |
|---|---|
| 产品定位、核心能力、适用场景 | https://www.workbuddy.cn/docs/workbuddy/Overview |
| 技能（Skill）：定义、技能市场与已安装、添加/启用关闭/搜索/卸载、安全提示 | https://www.workbuddy.cn/docs/workbuddy/From-Beginner-to-Expert-Guide/Function-Description/Skills-Market |
| 专家与专家团：角色切换 vs 协作执行、召唤与置顶、Skill vs 专家 vs 专家团 对比表 | https://www.workbuddy.cn/docs/workbuddy/From-Beginner-to-Expert-Guide/Function-Description/Expert-Center |
| 人机双写：文档内 AI 编辑 vs 手动编辑、选区精调、权限边界、积分说明 | https://www.workbuddy.cn/docs/workbuddy/From-Beginner-to-Expert-Guide/Function-Description/Knowledge-Base/Cowriting |
| SKILL.md 目录结构与 frontmatter 字段 | https://www.workbuddy.cn/docs/ide/Features/Skills |

> ⚠️ **路径陷阱（2026-09-17 实测）**：官方文档的 `Practice-Cases` 分组（如 `.../Practice-Cases/Document-Generation`）
> 在 `www.workbuddy.cn` 上返回 **404**，只在 `www.workbuddy.ai` 上存在。
> 引用前先确认页面能打开，**不要写没验证过的 URL**。
>
> 另：第三方转载（ima 知识库、CSDN、云厂商文档等）内容常常更详细，但**不能作为本站的官方口径依据**——
> 只用于帮助我们理解，落笔时仍以官方页面为准。

### 11.2 补充素材源：WorkBuddy 实战蓝皮书（非官方）

`https://workbuddy.homes/bluebook/` — 全书 4 篇 28 章，以真实任务为主线，
内容比官方文档细，尤其**操作流程与实践建议**部分很有价值。

**已抓取并整合的章节（2026-09-17）**：

| 蓝皮书章 | 对应课次 | 整合到哪一节 |
|---|---|---|
| 第 1 章 初识 WorkBuddy | 第 7 课 | 作为背景，未单独成节（内容与官方「简介」页重合） |
| 第 3 章 主界面、任务与工作区 | 第 7 课 | 「界面三大区域」补「每区该检查什么」表；**新增**「工作空间与安全边界」「选模型」两节 |
| 第 4 章 快速完成第一个任务 | 第 7 课 | 「第一个任务的正确打开方式」补六要素清单（在四要素基础上多「动作」「验收」） |
| 第 5 章 加载一个用得上的 Skill | 第 8 课 | **新增**「它是怎么被用上的：渐进式披露」一节 |
| 第 6 章 专家和专家团 | 第 9 课 | 「怎么选」补四层对比（含「普通任务」）；「专家团」补「召唤前四问」 |

**待用**：第 7 章连接器 / 第 9 章小程序与 IM 助理 / 第 10 章外部 API / 第 11 章自动化任务，
对应第 16、18 课，等转化那几课时再抓。

**使用纪律（三条，务必遵守）**：

1. **产品口径必须回官方复核**。蓝皮书里涉及界面名称、功能行为、权限边界的说法，
   一律先去 `www.workbuddy.cn` 核对；只有**纯经验性建议**（如「按任务建目录」「拿不准就用自动模式」）
   可以直接引用，并在页面注明「整理自 WorkBuddy 实战蓝皮书」。
2. **事实性陈述必须独立核实**。本次已核实一条：蓝皮书称「Anthropic 于 2025 年 10 月推出 Agent Skills，
   2025 年 12 月发布为开放标准」——经查 Anthropic 官方发布页，标注为 **2025-10-16 推出、2025-12-18 发布为开放标准**，属实。
   页面已按官方日期写入。
3. **与官方冲突时以官方为准**，并在页面上说明差异，不要两处各说一套。


